{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "scripts": {
    "dev": "json-server --watch db.json --port 9000"
  },
  "dependencies": {
  "json-server": "^0.17.4",
  "path": "^0.12.7"
}
}
